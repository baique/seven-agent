# LLM 模型配置

此目录存放 LLM 模型配置文件（.json 格式）。

## 示例

```json
{
  "model": "deepseek-chat",
  "api_key": "sk-xxx",
  "base_url": "https://api.deepseek.com/v1"
}
```

## 说明

- 每个模型一个 `.json` 文件
- 文件名即模型名称（如 `deepseek-chat.json`）
- 此目录内容不会被提交到 Git
